{
  "name": "helperscripts",
  "scripts": {
    "runAllTestDependencies": "docker-compose up authDb groceryDb redis mongo adminer",
    "composeUp": "docker-compose  --env-file env_variables/dev/.env.docker-compose up"
  }
}
