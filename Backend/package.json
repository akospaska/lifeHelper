{
  "name": "helperscripts",
  "scripts": {
    "runAllTestDependencies": "docker-compose up authDb groceryDb rabbitmq redis mongo adminer",
    "composeUp": "docker-compose  --env-file env_variables/dev/.env.docker-compose up"
  }
}
